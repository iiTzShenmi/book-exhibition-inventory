# Optional helper-tool dependencies.
#
# Install this only when running one-off import/scraping/diagnostic tools:
#   python -m pip install -r requirements-tools.txt
-r requirements.txt
openpyxl==3.1.5
Pillow==12.2.0
